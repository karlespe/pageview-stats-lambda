# Pageview Statistics Via AWS Lambda

JavaScript application that records pageview statistics on a Wikipedia-like user interface via AWS Lambda. Backend makes use of AWS APIs including Lambda and DynamoDB.
